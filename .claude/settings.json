{
    "hooks": {
        "PostToolUse": [
            {
                "matcher": "Write|Edit",
                "hooks": [
                    {
                        "type": "command",
                        "command": "jq -r '.tool_input.file_path' | { read file_path; if echo \"$file_path\" | grep -qE '\\.(js|ts|jsx|tsx|vue|css|scss|html|json|md|yml)$'; then bun run prettier --write \"$file_path\"; fi; }"
                    }
                ]
            }
        ]
    }
}
