name: Release

on:
    push:
        tags:
            - 'v*'

# Required permissions for npm provenance
permissions:
    contents: write # Create GitHub releases
    id-token: write # Publish with provenance

jobs:
    release:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v6

            - name: Setup Node.js
              uses: actions/setup-node@v6
              with:
                  node-version: '22'
                  registry-url: 'https://registry.npmjs.org'

            - name: Setup Bun
              uses: oven-sh/setup-bun@v2
              with:
                  bun-version: latest

            - name: Install dependencies
              run: bun install --frozen-lockfile

            - name: Build standalone binary
              run: bun run build && mv dircmp dircmp-linux-x64

            - name: Publish to npm
              run: npx npm@latest publish --provenance --access public

            - name: Extract changelog
              id: changelog
              run: |
                  version="${GITHUB_REF_NAME#v}"
                  body=$(awk "/^## \\[${version}\\]/{found=1; next} /^## \\[/{if(found) exit} found" CHANGELOG.md)
                  {
                    echo "body<<EOF"
                    echo "$body"
                    echo "EOF"
                  } >> "$GITHUB_OUTPUT"

            - name: Create GitHub Release
              uses: softprops/action-gh-release@v2
              with:
                  body: ${{ steps.changelog.outputs.body }}
                  files: dircmp-linux-x64
